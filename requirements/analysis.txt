-r core.txt

# Optional plotting, geospatial, and higher-level analysis tools.
colorcet>=3
matplotlib>=3.8
rasterio>=1.3
xarray>=2024.1

# Some case-study tools additionally need ecCodes, xESMF/ESMF, HORAYZON, or
# site-provided modules. Those are environment-specific and intentionally not
# forced into the portable local environment.
